<h1><%= @cohort.name %></h1>

<h3>Enrolled Students</h3>
<ul>
  <% @cohort.students.each do |student| %>
    <li><%= student.name %></li>
  <% end %>
</ul>

<a href="/cohorts/<%= @cohort.id %>/edit">Edit</a>
